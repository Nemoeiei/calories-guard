[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: hits a real Postgres; skipped by default. Run with `-m integration`.
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
